{
  "name": "sleep-compiler",
  "version": "1.1.0",
  "description": "Your personal sleep schedule compiler — log, analyze, and improve your sleep",
  "type": "module",
  "bin": {
    "sleep-compiler": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "node --import tsx/esm src/index.ts",
    "test": "node --import tsx/esm --test test/**/*.test.ts",
    "prepublishOnly": "tsc"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.11.30",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  }
}
